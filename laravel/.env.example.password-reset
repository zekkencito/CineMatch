<?php

/**
 * Ejemplo de variables de entorno (.env)
 * para la funcionalidad de recuperación de contraseña
 * 
 * Copia y actualiza estos valores en tu archivo .env
 */

return [
    // ===========================================
    // MAIL CONFIGURATION - Mailtrap (desarrollo)
    // ===========================================
    
    'MAIL_MAILER' => 'smtp',
    'MAIL_HOST' => 'smtp.mailtrap.io',
    'MAIL_PORT' => '465', // o 2525
    'MAIL_USERNAME' => 'tu_mailtrap_username',
    'MAIL_PASSWORD' => 'tu_mailtrap_password',
    'MAIL_ENCRYPTION' => 'tls',
    'MAIL_FROM_ADDRESS' => 'noreply@cinematch.com',
    'MAIL_FROM_NAME' => 'CineMatch',

    // ===========================================
    // MAIL CONFIGURATION - SendGrid (producción)
    // ===========================================
    // Descomentar si usas SendGrid
    /*
    'MAIL_MAILER' => 'sendgrid',
    'SENDGRID_API_KEY' => 'tu_sendgrid_api_key',
    'MAIL_FROM_ADDRESS' => 'noreply@cinematch.com',
    'MAIL_FROM_NAME' => 'CineMatch',
    */

    // ===========================================
    // MAIL CONFIGURATION - Mailgun (producción)
    // ===========================================
    // Descomentar si usas Mailgun
    /*
    'MAIL_MAILER' => 'mailgun',
    'MAILGUN_SECRET' => 'tu_mailgun_secret',
    'MAILGUN_DOMAIN' => 'tu_mailgun_domain',
    'MAIL_FROM_ADDRESS' => 'noreply@cinematch.com',
    'MAIL_FROM_NAME' => 'CineMatch',
    */

    // ===========================================
    // Frontend URL
    // ===========================================
    // URL donde está tu aplicación React Native web o sitio frontend
    'FRONTEND_URL' => 'http://localhost:3000', // desarrollo
    // 'FRONTEND_URL' => 'https://cinematch.com', // producción

    // ===========================================
    // Token de Reset Configuration
    // ===========================================
    'PASSWORD_RESET_TOKEN_EXPIRY' => 3600, // 1 hora en segundos
    'PASSWORD_RESET_TOKEN_LENGTH' => 60, // Caracteres

    // ===========================================
    // Security
    // ===========================================
    'APP_DEBUG' => true, // false en producción
    'APP_URL' => 'http://localhost:8000', // URL del backend
];

/**
 * PASOS PARA CONFIGURAR:
 * 
 * 1. Copia estos valores a tu archivo .env:
 *    cp .env.example .env
 *    nano .env
 * 
 * 2. Si usas Mailtrap:
 *    - Regístrate en https://mailtrap.io
 *    - Copia credenciales de Dashboard > Inboxes > Your Inbox > SMTP Settings
 * 
 * 3. Si usas Producción (SendGrid/Mailgun):
 *    - Obtén API key del proveedor
 *    - Reemplaza SMTP con el servicio elegido
 *    - Ejecuta: composer require laravel/sendgrid-transport (si usas SendGrid)
 * 
 * 4. Verifica la configuración:
 *    php artisan config:cache
 *    php artisan queue:work (si usas queue para emails)
 * 
 * 5. Para envios asincronos (recomendado):
 *    En PasswordResetMail.php, agrega: implements ShouldQueue
 */
