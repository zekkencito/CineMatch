{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd laravel && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd laravel && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
